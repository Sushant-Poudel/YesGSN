# Frontend environment variables
# Copy to .env.local for local dev; set the same keys in Vercel > Project > Settings > Environment Variables for production.

# Public URL of the FastAPI backend (no trailing slash)
NEXT_PUBLIC_BACKEND_URL=https://api.gameshopnepal.com

# Google OAuth client ID used by @react-oauth/google
NEXT_PUBLIC_GOOGLE_CLIENT_ID=
